<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production config: no cleartext traffic permitted to any host.
        Debug builds use src/debug/res/xml/network_security_config.xml which
        additionally permits cleartext to local emulator/dev addresses.

        TODO: Add certificate pinning for alignify.shoryadhyani.me before the current
        cert expires (2026-05-05). Obtain the SPKI pin with:

          echo | openssl s_client -connect alignify.shoryadhyani.me:443 2>/dev/null \
            | openssl x509 -pubkey -noout \
            | openssl pkey -pubin -outform DER \
            | openssl dgst -sha256 -binary | base64

        Then add here (include a backup CA pin to avoid lockout on cert rotation):

        <domain-config>
            <domain includeSubdomains="true">alignify.shoryadhyani.me</domain>
            <pin-set expiration="YYYY-MM-DD">
                <pin digest="SHA-256">LEAF_SPKI_BASE64</pin>
                <pin digest="SHA-256">BACKUP_CA_SPKI_BASE64</pin>
            </pin-set>
        </domain-config>
    -->
</network-security-config>
